---
title: "Self-Hosting & Advanced Setup"
description: "Run Flumes Memory on your own infra."
---

<Info>
Self-hosting is currently **invite-only**. Contact us if you require on-prem deployment.
</Info>

## Local dev

```bash
# 1. Clone repo
$ git clone https://github.com/flumes-ai/flumes
# 2. Start services
$ docker compose up -d --build
# 3. API available at
$ curl http://localhost:8000/v1/health
```

## Swap vector store

Edit `FLUMES_VECTOR_STORE` env var (`faiss`, `pgvector`, `pinecone` …) and restart the container.

## Production checklist

- Use Postgres for persistence
- Enable `TLS` in your load balancer
- Rotate your `SECRET_KEY`
